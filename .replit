run = "cd backend && npm install && npm start"

modules = ["nodejs-20"]

[deployment]
build = ["sh", "-c", "cd backend && npm install"]
run = ["sh", "-c", "cd backend && npm start"]

[[ports]]
localPort = 3001
externalPort = 80
